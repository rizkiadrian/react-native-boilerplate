/**
 * Available local redux toolkit key
 */
export const REDUX_KEY = {
  AppSession: 'appSession',
};

/**
 * API Base url and suffix added on axios base file as base url
 */
export const API_BASE_URL = 'https://api-mock.clivearts.com';
export const API_SUFFIX = 'api/rz-travel/v1';

/**
 * API reducer prefix used as reducerpath in RTK query
 */
export const API_REDUCER_PATH_PREFIX = 'rztravel_api_';
